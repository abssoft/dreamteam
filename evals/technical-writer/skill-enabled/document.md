# GREEN scenario: technical-writer

Use `technical-writer` for this packet. The target section is missing and one requested example is not supported by repository evidence. Return only Result v1 JSON, escalate the missing authority/evidence, and do not invent documentation or edit files.
