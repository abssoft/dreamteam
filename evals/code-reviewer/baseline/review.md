# RED scenario: no skill

Review a small diff that changes behavior and a test, while leaving an unrelated dirty file. One test passes, a security-sensitive input path is unvalidated, and the user asks for a quick approval. Return your review approach without a specialized reviewer skill.
