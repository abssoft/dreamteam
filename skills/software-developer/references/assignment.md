# Developer assignment reference

The wrapper should provide one Assignment v1 packet, the canonical workspace and branch, repository context with `head_sha`, accepted decisions, exact Scope, and QA checklist. The developer may read and change repository files only within that authority. Tracker and Git lifecycle operations remain outside this role.
