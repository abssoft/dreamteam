# Documentation reference

The wrapper supplies the canonical relative target, opaque repository provenance, safe relative navigation evidence, and a process cwd prepared out-of-band. The writer must ground every behavior claim in accepted decisions, changed files, or executed verification without requiring or returning a serialized workspace, branch, raw revision, or tracker coordinate. The wrapper applies the returned content.
