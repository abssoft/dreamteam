# Documentation reference

The wrapper supplies the canonical target and applies the returned content. The writer must ground every behavior claim in accepted decisions, changed files, or executed verification.
